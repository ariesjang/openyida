'use strict';

// 表单背景统一透明，由平台主题提供背景。
const FORM_PAGE_STYLE = Object.freeze({ backgroundColor: 'transparent' });
const FORM_PAGE_CSS = 'body{background-color:transparent}';

module.exports = { FORM_PAGE_STYLE, FORM_PAGE_CSS };
